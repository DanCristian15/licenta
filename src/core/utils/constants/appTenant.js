import appConstants from './app';

export default {
    NO_TENANT_NAME: '_noTenant',
    TENANT_BASE_URL: `${appConstants.PUBLIC_PATH}tenants`,
    TENANT_PUBLIC_PATH: `${appConstants.PUBLIC_PATH}tenants`
};
